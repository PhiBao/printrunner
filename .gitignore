# secrets / env
.env
*.key

# data & artifacts (data/ is runtime state, lives in Supabase for prod)
data/
# built dashboard contains the Supabase anon key at deploy time — never commit;
# publish with `vercel deploy` (uploads the file, does not use git)
docs/index.html
.pytest_cache/
__pycache__/
*.pyc
.venv/
*.egg-info/
build/
dist/
.DS_Store
# presentation intermediates (generated, large)
presentation/audio/
presentation/clips/
presentation/frames/
presentation/concat.txt
presentation/*.mp4
presentation/*.pdf
!presentation/PrintRunner.mp4
!presentation/PrintRunner.pdf
worker/.wrangler/
.vercel
.env*
